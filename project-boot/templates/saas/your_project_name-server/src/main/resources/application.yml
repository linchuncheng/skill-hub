server:
  port: 8083

spring:
  application:
    name: your_group_name-your_project_name
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.your_group_name.your_project_name.entity

logging:
  level:
    com.your_group_name: DEBUG
